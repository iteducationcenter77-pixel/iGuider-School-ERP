/**
 * iGuider School ERP — Supabase Configuration (EXAMPLE)
 * ======================================================
 * Copy this file as `supabase-config.js` and fill in your real keys.
 * The actual `supabase-config.js` is git-ignored for security.
 *
 * Steps:
 *  1. Go to https://supabase.com → your project → Settings → API
 *  2. Copy the "Project URL" and "anon public" key
 *  3. Paste them into your `supabase-config.js`
 */

window.IGUIDER_SUPABASE = {
  url: "https://YOUR_PROJECT_ID.supabase.co",
  anonKey: "YOUR_SUPABASE_ANON_KEY"
};
